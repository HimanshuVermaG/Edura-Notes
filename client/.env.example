# Backend API URL. Leave empty for local dev (Vite proxy) or same-origin. Set for hosted backend (e.g. Vercel).
# Example: https://your-api.vercel.app
VITE_API_URL=

# Google Sign-In (same OAuth 2.0 Web client ID as server GOOGLE_CLIENT_ID)
VITE_GOOGLE_CLIENT_ID=
